#include "main.h"
/**
 * _isupper - Checks if a charachter is a digit
 * @c : The character tested
 * Return: Nothing
 */
int _isdigit(int c)
{
		if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
