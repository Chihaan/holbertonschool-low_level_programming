#include "main.h"
/**
 * _isupper - Checks if a charachter is uppercase
 * @c : The character tested
 * Return: Nothing
 */
int _isupper(int c)
{
		if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
}
