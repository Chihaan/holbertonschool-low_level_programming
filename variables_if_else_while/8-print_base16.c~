#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;
		for (ch = '0'; ch <= 'F'; ch++)
		{
			putchar(ch);
		}
		putchar('\n');
	return (0);

}
