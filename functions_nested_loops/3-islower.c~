#include "main.h"
/**
 * _islower - vérifie si un caractère est en minuscule ou majuscule
 * @l: Le caractère a vérifier
 * Return value 1 si vrai 0 si faux
 */
int _islower(int l)
{
	if (l >= 'a' && l <= 'z')
		return (1);
	else
		return (0);
}
